export const onlyText = () => `This is a simple message.`

export const oneParam = (params: any) => `Hello ${params.name}!`

export const multipleParams = (params: any) =>
	`Hello ${params.name}! You have ${params.count} Messages.`
